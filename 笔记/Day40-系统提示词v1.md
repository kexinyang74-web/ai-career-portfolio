# Day 40 系统提示词 v1（第 6 周第 5 天）

> 在现有 `projects/project-1-ai-assistant/system_prompt.md` 上打磨，**不另起炉灶、不改 README 第一行定位句**。v1 验收 = 你能指着每个标题说「为什么有这一段」。**正文你改，我不代写。**

`main.py` 启动时会把该文件整份读进 `system` 消息。改文件后重新跑 `python main.py` 才生效（已开的交互窗口仍是旧人设）。

## 现有稿对应四件套（先标再改）

| 件 | 现在大概在哪 | Day 39 暴露的问题（供你决定要不要改） |
|---|---|---|
| 角色 | 开头「选题与脚本助手」+ 人设引号 | 人设已定，不要改成别的行业 |
| 任务 | 「你怎么干活」里 10+3、口播三段 | 用户没写条数时才会走默认 |
| 约束 | 「边界」+ 不编造播放量 | 跑题拉回仍有效 |
| 格式 | 10+3、钩子/中段/CTA | 「再补一句为什么适合」会在清单后面长出来（你 B 版见过） |

文首可加一行版本说明，例如：`v1（2026-09-05）`，方便以后对照。

## 今天你必须自己做的两件事

1. **改 `system_prompt.md`**：只拧你讲得清的螺丝。建议至少想清楚下面这一条（写进文件或日志均可）：用户已经用四件套写了条数/格式时，默认 10+3 和「再补一句」是让还是让路？
2. **跑一次验收**：同一句糊 user（例如 `帮我想几个选题`），看 v1 默认产出是否仍是你想要的清单，而不是又变回小作文。不要改 `.env`，不要 push `.env`。

不要把 few-shot 长例子整段塞进 system（那是某一轮 user 的事）。system 放稳定规则。

## 明确不做

- 不改 `main.py` 大结构
- 不改 README 定位句（面向 B站 选题与脚本助手）
- 不上 RAG、不录演示（第 7 周）

## 做完

答 `笔记/Day40-自测5题.md`；日志里用自己的话写：你动了哪几段、为什么。改完可以把文件发我批，我只对照四件套勾，不替你重写。
