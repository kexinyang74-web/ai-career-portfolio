# Day 65 跑评测（第 10 周第 2 天）

> 把表里的问句真正丢给 `project-2-rag`。默认先跑 **前 10 条**（省时间和钱）。引用命中脚本会按文件名猜；**可接受必须你看回答**。

## 今天你做

```powershell
cd C:\Users\Administrator\Desktop\学习安排\projects\project-2-rag
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
python eval_run.py
```

跑满 30 条：

```powershell
python eval_run.py --all
```

看 `eval/运行记录.md`。把你判定的「可接受 / 引用命中」填回 `eval/评测集.md`（脚本猜的引用可以改）。

## 怎么勾

- 可接受：依据笔记；不足就说不足。编了没有的标题/结论 → 否
- 引用命中：预期 `02.md` 时回答里要有 `02.md`；预期「无」时不应拿一篇 md 冒充「库里有这条」
- 第 24–26 条：备注写了「至少命中其一」，不要用「必须只出现一篇」卡死

## 明确不做

- 不调 k、不重切、不改 prompt（那是 Day 66）
- 不把运行记录里的密钥贴到聊天（脚本本来也不会打印密钥）

## 做完

答 [[Day65-自测5题]]。说「收尾」再批。
