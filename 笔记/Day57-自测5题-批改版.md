# Day 57 · 自测 5 题 · 批改版

> 对照 `01_chroma.py`：JSON 向量写入 Chroma，问句再 Embed，`query` 取 top-k。自测 **5✅**。

---

## 第 1 题：对照

**批改：✅** 上周 `embeddings.json`；今天 `day57/chroma_db/`。写在磁盘上，`PersistentClient` 再打开还能查。JSON 也是落盘，Chroma 多了索引和检索。

---

## 第 2 题：手写余弦

**批改：✅** 查询不必再写 `dot/(na*nb)`，Chroma 内部算 distance。你的脚本走的是 `query_embeddings`（问句向量），不是 `query_texts`。

---

## 第 3 题：问句

**批改：✅** 问句是字，要比必须先变成同一模型、同一空间的向量。切块向量是从 JSON add 的，问句仍要调 Embedding。

---

## 第 4 题：distance

**批改：✅** 越小越好；和上周余弦（越大越好）方向相反。

---

## 第 5 题：还没做的

**批改：✅** 无 LangChain、无 Chat、无 `where` 过滤。过滤是明天。

---

日志已代填，标待确认。未开 Day 58。
