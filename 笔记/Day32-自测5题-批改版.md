# Day 32 · 自测 5 题 · 批改版

> 对照原文。脚本已跑通（终端 exit 0，字是边蹦出来的）。**4 题 ✅，1 题 ⚠️**（第 3 题漏了「怎么拼完整句」；第 4 题函数对、命令没写）。

## 第 1 题：两个 stream

**批改：✅** 服务器 SSE vs 客户端不要一次读完，只开一边都不是真流式。今天最硬的一条。

## 第 2 题：路径

**批改：✅** `delta.content` + `data: [DONE]`。完整下标是 `choices[0]["delta"].get("content")`，和 `_consume_stream` 一致。

## 第 3 题：拼完整回复

**你的答案：** 只解释了 `end=""` / `flush=True`。

**批改：⚠️** 这两参数对（不换行 + 立刻显示）。题目还问**一整段话从哪来**：把每片 `piece` 放进列表，最后 `"".join(chunks)`。`flush` 只管显示，不管拼字符串。

## 第 4 题：骨架

**你的答案：** `_consume_stream`、`call_llm`。

**批改：⚠️→ 函数 ✅。** 真正读 SSE 的是 `_consume_stream`；`call_llm(..., stream=True)` 负责把开关传进去。交互里打开流式的命令是 **`/stream`**（你没写）。

## 第 5 题：现象

**批改：✅** 边生成边出现；四句短句与终端第二次运行一致。

Day 32 实践 + 概念过关，已打卡。
