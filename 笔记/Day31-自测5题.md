# Day 31 · 自测 5 题（作答模板）

> 先跑通 `01_multi_turn.py` 再答。

## 第 1 题：记忆在哪

大模型两次 HTTP 请求之间「记得」你的名字，是因为它存在服务器上，还是因为你把什么又寄回去了？

你的答案：
因为我把第一轮的对话历史寄回去了。大模型没有记忆

## 第 2 题：append 顺序

一轮对话结束后，`messages` 里应该按什么顺序追加？漏掉 `assistant` 那一条，第二次提问会发生什么？

你的答案：
messages.append({"role": "assistant", "content": reply1})

messages.append({"role": "user", "content": "我叫什么名字？只回答名字。"})
第二次回答和第一次一样，只回复了好

## 第 3 题：/new

项目一输入 `/new` 时，`history` 变成什么样？它解决的是费用问题、窗口问题，还是两者都有？

你的答案：
`history`会清空
两者都有

## 第 4 题：token

你两次运行打印的 `prompt_tokens` 分别是多少？为什么第二次通常更大？

你的答案：
21 43
因为第二次会包含之前一轮的对话

## 第 5 题：和骨架对应

`main.py` 的 `chat_repl` 里，`call_llm(history)` 之前和之后各 `append` 了什么 `role`？

你的答案：
 history.append({"role": "user", "content": user_input})
 history.append({"role": "assistant", "content": reply})