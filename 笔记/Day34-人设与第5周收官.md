# Day 34 人设 + 力扣 + 第 5 周收官

> 第 5 周最后学习日。三件事：① 人设改成选题助手并跑通 ② 力扣 2 道 ③ 检查点自测 + 周复盘。人设草稿已按你 Day 5 的定位写进 `system_prompt.md`，**请你改到自己满意再跑**（协作标准：行业话必须是你的）。

## 一、人设（先改再跑）

文件：`projects/project-1-ai-assistant/system_prompt.md`  
README 第一行已改成：面向自媒体创作者（B站）——选题与脚本助手。

```powershell
cd C:\Users\Administrator\Desktop\学习安排\projects\project-1-ai-assistant
.\.venv\Scripts\activate
python main.py "赛道：转行学编程。按我的人设给 10 个选题和 3 个爆款开头。"
```

验收：回复应像 B站 助手，不再自称「电商运营顾问」。觉得哪句不像你，改 `system_prompt.md` 再跑。

## 二、力扣（本周 2 道，做透）

在 `projects/python-practice/day34/` 写，日志各写一句思路：

- **383. 赎金信**：`magazine` 的字母够不够组成 `ransomNote`（哈希计数）。
- **387. 字符串中的第一个唯一字符**：返回第一个只出现一次的字符下标，没有则 -1。

先自己写，能跑 `if __name__` 里的例子再交。

## 三、检查点自测 + 周复盘

- 答 `笔记/Day34-检查点自测5题.md`（对应本周 6 条检查点里的口头部分）
- 周复盘 4 问写在 `plan/第5周-进度追踪.md` 底部（本人填）
- 日志 `学习日志/2026-09-04-Day34-学习日志.md`

不要提交 `.env`、不要把密钥写进日志。
