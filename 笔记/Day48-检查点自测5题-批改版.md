# Day 48 · 第 7 周检查点自测 · 批改版

> 对照作答模板、day48 力扣（本地 3/0、3/2）、本周 CLI/README/三场/录屏/STAR。自测 **5✅**。**周复盘四问仍空，不代写；第 7 周收官差这一块。**

---

## 第 1 题：必须功能（核心）

**批改：✅** 多轮、流式、人设、`/new` `/save` 点过；错误提示在代码里、没专门演示；没做自动重试。末句「不要把没复现写成没有提示」是批注口吻，留在自测里即可，不必贴进周复盘。

---

## 第 2 题：一条命令

**批改：✅** venv → pip → 复制 `.env.example` → 自己填 key → `python main.py`。密钥只在 `.env`，不进 git。

---

## 第 3 题：架构

**批改：✅** 读内存 `history`；`/new` 后仍说上一轮是胡编，不是程序没清。

---

## 第 4 题：三种场景

**批改：✅** 不是三个行业。减肥出选题 / 改第 3 条生活记录 / 口播三段。

---

## 第 5 题：STAR 与演示

**批改：✅** 不能写错误重试；视频是出选题。

---

## 力扣

- **771**：扫 `stones`，`stone in jewels`，例子 3 / 0 通过。（也可用 `set(jewels)`，结果一样。）
- **485**：遇 1 加、遇 0 清零并 `max`；最后 `return max(max_count, count)` 能罩住数组以 1 结尾，对。

日志思路仍空，已按代码整理一句，待确认。周复盘请你填四问后再说一声，才能勾收官、改 `04` 表。
