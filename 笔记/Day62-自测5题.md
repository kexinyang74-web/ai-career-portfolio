# Day 62 · 自测 5 题（作答模板）

> FAISS 跑过再答。不要写密钥。

## 第 1 题：今天干什么

FAISS 脚本有没有重新切 10 篇、重新给每篇 Embedding？它读的是哪份文件？有没有调 Chat？

你的答案：
没有重新切 10 篇，也没有给每篇再 Embedding。 切块和向量还是 Day 51 / Day 52 那批。

读的是 `projects/python-practice/day52/embeddings.json`。问句仍要调一次百炼 Embedding，才能和库里的向量比。

没有调 Chat。 今天只检索、打印 top-k，没有 `/chat/completions`。

## 第 2 题：分数方向

今天 FAISS 打印的分数是越大越好还是越小越好？和 Chroma 的 distance 比，方向一样吗？

你的答案：


## 第 3 题：元数据

`source: 02.md` 是 FAISS 索引里自带的，还是我们列表里对齐下标带出来的？用 FAISS 做「只要 02.md」会比 Chroma 麻烦在哪？

你的答案：


## 第 4 题：三家

用自己的话：这条约 10 篇的项目二，为什么继续用 Chroma 就够？什么规模才值得提 Milvus？FAISS 适合补哪一块？

你的答案：


## 第 5 题：还没做的

今天有没有部署 Milvus？有没有把 `project-2-rag` 的默认库换成 FAISS？明天检查点要讲的核心是哪件事？

你的答案：
