# Day 54 · 自测 5 题 · 批改版

> 对照终端有无 RAG 两段。自测 **4✅ 1⚠️**。未单独说「收尾」，因你要求下一天，批改一并做完。

---

## 第 1 题：放哪

**批改：⚠️** 放到本轮 `user` 对。理由只写「省 token、不常需要」不完整。更准：片段是**这一问的资料**，跟人设/规则分开；人设留 `system`，资料跟问题走。全文塞人设会每次白带不相关篇，token 变多是后果。

---

## 第 2 题：引用

**批改：✅** 文件名让人能对到某一篇；检查点 = 能检索 + 能引用。无检索给不出 `02.md`。

---

## 第 3 题：对比

**批改：✅** 无检索看不见笔记；有检索用你的钩子/为什么火，并点名 `02.md`。这次两边都少胡编标题。

---

## 第 4 题：仍会编

**批改：✅** 会。你举的 top-k 噪音（`03.md`/`05.md` 进 `user`）就是今天流水线里的真实风险。

---

## 第 5 题：两套 API

**批改：✅** DeepSeek Chat vs 百炼 Embedding，BASE 和 Key 都分开，原因对。

---

日志已按终端代填，标待确认。
