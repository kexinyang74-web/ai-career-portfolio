# Day 43 · 自测 5 题 · 批改版

> 对照作答模板、终端（含 `/new` 后仍说「上一轮」）、`main.py`。自测 **4✅ 1⚠️**。CLI 点名按你日志和终端算完成。

---

## 第 1 题：多轮

**批改：⚠️** `/new` 之后接不上、还会胡编，后半对。前半把两件事混在一起了。

- **同一轮会话里第二句还能接到选题**：不是假象。`history` 里真的还装着上一句 user 和 assistant，这次 POST 会带上去。
- **`/new` 之后还说「上一轮 10 条」**：才是假象。内存已只剩 system，模型根据「第 5 个」+ 人设里的 10+3 /「按上一轮改」在演。

要限制的话，改 system 里 **已有的「多轮时根据上一轮修改」那一句**（历史里没有清单就不要假装记得），不必新开一章。你还没说「改」，文件先不动。

---

## 第 2 题：流式

**批改：✅** 切的是 `chat_repl` 里的 `stream`（True/False）。拼字走到 `delta` / `content` 即可。项目一流式是先读 SSE 的 `data:` 行再 `json.loads`，和你写的 `choices[0]["delta"]["content"]` 是同一条路径。

---

## 第 3 题：保存与 git

**批改：✅** 文件是 `chat_history.json`（你写成 `chat_history` 也认）。隐私 vs 密钥/资金，两句话没混。`/new` **不删**这个文件，只清内存。

---

## 第 4 题：错误处理 vs 重试

**批改：✅** 失败即打印并 `sys.exit(1)`，无自动再 POST。本周不做、先保演示，和规格「必须功能」里已有错误提示一致。简历若写「错误重试」就口说无凭，以后加再改简历。

---

## 第 5 题：README

**批改：✅** 示例仍是推广文案 / 销量下滑，不是选题。今天只验收 CLI，改示例是 Day 44。

---

日志「遇到的问题」保留你的原文；原理格已按你的发现整理进日志，不是另编一套。
