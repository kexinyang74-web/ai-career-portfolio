# Day 55 第 8 周收官（力扣 + 检查点 + 周复盘）

> 检查点核心：**不靠 LangChain，能讲清三个词，并手写跑通 读文件→切分→Embedding→检索→拼进 prompt**；约 10 篇能问答；有一次无 RAG vs 有 RAG。周复盘四问自己写。不要 push `.env`。

## 一、力扣 2 道（**已跳过**）

用户 2026-09-10 明确跳过。`day55/leetcode_268.py`、`leetcode_455.py` 骨架仍在，以后要补再自己写 `...` 跑通。

用户当日跳过 268/455 后，**力扣改为每两个月一次**（不再作为每周检查点）。RAG 核心检查点不受影响。下次力扣约 2026-11。

## 二、检查点自测

答 [[Day55-检查点自测5题]]。对照你跑过的 `01_split.py` / `01_embed.py` / `01_search.py` / `01_rag_chat.py` 和有无检索那两段输出。

## 三、周复盘

填日志四问 + `plan/第8周-进度追踪.md` 底部四问。04 表第 8 周行等「收尾」时再填；力扣未做，不要把检查点写成 6/6 全过。

下周（第 9 周）才是 Chroma / LangChain / 项目二工程化。今天不要开始装 `chromadb`。

## 明确不做

- 不上 Chroma/LangChain、不凑 40 篇、不改项目一人设
- 不提交 `.env` / `embeddings.json`（可 gitignore）
