# Day 67 全量复测 k=5（第 10 周第 4 天）

> 同一套 30 条，只把 k 换成 5。和 k=3 的 29/30 对照。不要覆盖 `eval/运行记录.md`。先不要改 `main.py` 默认 k。

## 今天你做

```powershell
cd C:\Users\Administrator\Desktop\学习安排\projects\project-2-rag
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
python eval_run.py --all -k 5 --out eval/运行记录-k5.md
```

跑完看：

1. 第 24 条是否仍有 `02.md`（Day 66 抽样已有，全量再确认）
2. 第 1 条（转行晚不晚）有没有把 `03.md` 写进主答
3. 自动引用命中是多少；抽几条看可接受有没有掉

对照表：

|          | k=3 基线         | k=5 今天            |
| -------- | -------------- | ----------------- |
| 记录文件     | `eval/运行记录.md` | `eval/运行记录-k5.md` |
| 第 24 条检索 | 10 / 05 / 01   | 10/05//           |
| 自动引用命中   | 27/30          | （填）               |

## 怎么决定默认 k

- 第 24 条好了，其它题没有明显变吵 → 可以考虑把 `main.py` 默认改成 5（改之前先说一声或自己改）
- 第 24 条好了，但第 1 类题开始混篇 → **保持默认 3**，多篇问题再用更大的 k 或换问法

## 明确不做

- 不重切、不改 prompt、不覆盖 k=3 基线文件

## 做完

答 [[Day67-自测5题]]。说「收尾」再批。
