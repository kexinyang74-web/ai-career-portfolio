# Day 41 第 6 周收官（力扣 + 检查点 + 周复盘）

> 第 6 周检查点核心：**能解释 prompt 里主要段落为什么这么写**，不是堆形容词。今天三件事都做完再算收官。**周复盘和力扣思路你自己写。** 不要 push `.env`。

## 一、力扣 2 道（接 383 / 387 的计数）

文件放 `projects/python-practice/day41/`。自己写 `class Solution`，本地跑通即可（和以往一样，不强制交网站）。

| 题 | 一句话 | 和本周的关系 |
|---|---|---|
| **389** 找不同 | `t` 比 `s` 多一个字母，找出它 | 计数 / 抵消，像 383 用掉字符 |
| **409** 最长回文串 | 用手上的字母能拼的最长回文长度 | 计数后「成对的都能用」 |

提示（不是答案）：389 可以数次数或异或；409 想想偶数次数全用、奇数次数最多留一个放中间。

## 二、检查点自测

答 `笔记/Day41-检查点自测5题.md`。对照本周笔记和 `system_prompt.md`，不要背。

## 三、周复盘

填学习日志里四问，以及 `plan/第6周-进度追踪.md` 底部「周复盘」。填完发我，我只批改、不代写复盘。第 7 周才是 README / 演示 / 简历 STAR，今天不要提前做那些。

## 明确不做

- 不改 `main.py` 大结构、不上 RAG、不录演示
- 不提交 `.env`
