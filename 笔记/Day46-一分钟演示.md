# Day 46 约 1 分钟演示（第 7 周第 4 天）

> 你已定：**只录「出选题」**，不硬塞场 2/3。检查点是能给人看清这是选题助手，不是剪辑课。推送视频等你发话。不要把 `.env`、密钥、完整 `chat_history` 录进画面。

## 分镜（按这个走就够）

1. 终端已在项目一目录、venv 已激活（不要录安装过程）
2. `python main.py` 进交互
3. 打 **一句** 出选题（可用你 Day 45 那句，或更短的「给转行学编程出 3 条，每条标题---钩子」）
4. 等三行标题出现就停；可选闪一下界面上的命令提示（`/new` `/stream` `/save`）
5. 总长大约 1 分钟以内，超时就切，不要追问

Windows 可用 Win+G（Xbox 游戏栏）录窗口，或手机对着屏幕。文件放本机即可，例如 `projects/project-1-ai-assistant/demo/`（目录可自建）。**不要**把视频提交进 git，除非你明确要求。

录不成：日志写原因 + 上面 1–4 的文字分镜，算「能脱稿讲」的半成品，Day 48 再决定是否补录。

## 明确不做

- 不写 STAR（Day 47）、不改 `main.py`、不上 RAG
- 画面里不要打开 `.env`

## 做完

日志写：文件路径（或「未录，分镜如下」）。答自测。要批改说「收尾」。
