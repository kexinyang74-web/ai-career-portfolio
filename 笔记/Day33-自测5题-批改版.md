# Day 33 · 自测 5 题 · 批改版

> 对照原文。终端后半段已跑通：单次中文选题、交互、`/stream`、`/save`、`/new`、`/exit`。本次：**3 题 ✅，2 题 ⚠️**。

---

## 第 1 题：两种启动

**批改：✅** 有参数 → `ask_once`；无参数 → `chat_repl`。

---

## 第 2 题：密钥从哪来

**批改：✅** 环境变量（`load_dotenv` 读 `.env`），不是写死在 py 里。空密钥 → 打印那句错误 → `sys.exit(1)`，和代码一致。

---

## 第 3 题：/stream 和 /new

**批改：✅** 改的是布尔变量 `stream`；`/new` 只剩 system 人设。

---

## 第 4 题：/save

**你的答案：** 写入 `chat_history.json`；怕泄露 API 密钥。

**批改：⚠️** 文件名对。不进 git 的主因是：**里面是对话原文**（隐私/业务内容）。API 密钥在 `.env`，一般不在这个 JSON 里。两个文件都被 gitignore，但理由不同。

---

## 第 5 题：跑通了吗

**批改：⚠️→ 实践 ✅。** 你贴的 `UnicodeEncodeError` 是**第一次**失败：某个 HTTP **头**（latin-1）里出现了中文，常见是密钥行粘进了中文，或系统代理环境变量带了中文。  
后面同一句选题已经打出中文；`chat_history.json` 里有「你好啊」和流式那轮。请在答案里写清：**先报错，后跑通**；试过的命令：`/stream` `/save` `/new` `/exit`。

---

## 日志

「今天做了什么」太短。原理「单词问题」应是 **单次**提问。`ask_once` 不记住下一句；`chat_repl` 用 `history` 多轮。

---

请补 1 句（聊天回即可）：`chat_history.json` 不进 git，主要是因为 ______（不是因为里面有密钥）。

补答：对话内容（隐私）✅

**自测 5/5 通过。** 密钥在 `.env`；对话在 `chat_history.json`。下一步 Day 34。
