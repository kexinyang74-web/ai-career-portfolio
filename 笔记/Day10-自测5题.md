# Day 10 · 容器（列表/字典/元组/集合）自测 5 题

> 作答规则：先自己写，不许翻笔记/文档；写不出来的地方标注"没掌握"。写完后发给 Claude 批改，批改后生成批改版。

## 第 1 题：列表增删改查（写代码）

创建一个列表 `nums = [1, 2, 3]`，依次完成：①末尾加一个 `4` ②把索引 1 的值改成 `10` ③删掉值 `2` ④打印最终列表和它的长度。

作答：
nums = [1,2,3]
nums.append(4)
nums[0] = 10
nums.remove(2)
print(nums)
print(len(nums))


## 第 2 题：字典增删改查（写代码）

创建一个字典 `user = {"name": "小明", "age": 20}`，依次完成：①打印 `name` 的值 ②加一个键 `city`，值是 `"北京"` ③把 `age` 改成 `21` ④删掉 `city` ⑤用 `.get()` 安全地取一个不存在的键（不报错，返回默认值）。

作答：
user = {"name": "小明", "age": 20}


## 第 3 题：元组 vs 列表（写代码 + 一句话）

写代码验证：`t = (1, 2, 3)` 尝试 `t[0] = 99` 会报什么错？再写一行代码把元组转成列表。最后用一句话说：**什么时候应该用元组而不是列表？**

作答：


## 第 4 题：集合去重（写代码）

`words = ["apple", "banana", "apple", "orange", "banana"]`，写代码得到去重后的列表（不要求顺序），并打印 `"apple" in 去重后` 的结果。提示：`list(set(words))`。

作答：


## 第 5 题：综合（写代码）

写函数 `count_char(text)`，统计字符串里**每个字符**出现的次数（用字典，类似笔记里的 word_freq）。调用 `count_char("hello")`，应输出类似 `{'h': 1, 'e': 1, 'l': 2, 'o': 1}`。

作答：

