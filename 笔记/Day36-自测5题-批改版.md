# Day 36 · 自测 5 题 · 批改版

> 对照原文。两次 `main.py` 已跑通（日志里有完整现象）。本次 **5/5**。

---

## 第 1 题：四件套

**批改：✅** 角色=谁、任务=产出什么、约束=不准/不确定怎么说、格式=长什么样。和笔记一致，能用自己的表复述即可。

---

## 第 2 题：system vs user

**批改：✅** 稳定人设和边界进 `system`；换赛道只改 `user`。这是今天最值钱的分工。

---

## 第 3 题：对照人设文件

**批改：✅** 角色那段、`## 边界` 都指对了。「10 + 3」算**默认任务**也对。补一句：条数本身也约束了格式（必须是那么多条）；它写在「你怎么干活」里，所以糊指令「帮我想几个选题」才会被 system **拉成 10+3 长文**。

---

## 第 4 题：和温度

**批改：✅** 温度管随机，补不了没写清的规则。

---

## 第 5 题：两次调用

**批改：✅** 现象说准了：糊的一次套 system 默认模板（又多又像范文）；结构那次 5 条一行一个钩子，更贴「转行学编程 + 不要鸡汤」。这就是 user 四件套压过了 system 里那条默认 10+3。

---

日志原理：提示词不只在 system，每次 user 也是 prompt。**这条感悟满分。**
