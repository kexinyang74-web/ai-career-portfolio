# Day 34 · 第 5 周检查点自测 · 批改版

> 对照原文。力扣本地例子已跑通（383：F F T；387：0 2 -1）。人设提问输出已是 B站 选题，不是电商。

**自测：4 题 ✅，1 题 ⚠️（上下文窗口）。周复盘和日志思路仍空，第 5 周还不能收官。**

---

## 第 1 题：一次请求

**批改：✅** POST `/chat/completions`、头里 Bearer、`model`+`messages`、`choices[0].message.content`。这是本周核心检查点。

---

## 第 2 题：三个词

**批改：⚠️**

- token：最小切分单位 ✅（补主语：模型在切）。
- temperature：下一个 token 多随机 ✅。
- 上下文窗口：不只是「对话有多长」。它是**一次请求里 messages + 即将生成的回复，加起来不能超过的 token 上限**。超了会截断或报错；`/new` 就是把占用清回去。

---

## 第 3 题：多轮 / 流式

**批改：✅** 不加 assistant 就不知道自己上轮说了啥；`delta.content` → `join` → `[DONE]`。

---

## 第 4 题：密钥 vs 历史

**批改：✅** `.env` 防盗用花钱；`chat_history.json` 防对话隐私。两句没混。

---

## 第 5 题：人设

**批改：✅** 服务你做 B站 选题；命令和回复已不是电商顾问。`system_prompt.md` 你改过「生活记录当主体」（Day 5 原定是钩子+干货，可以，记下即可）。

---

## 力扣（代码）

- **383**：逐字从 magazine 里 `replace` 掉一个，例子全对。能跑。以后可用计数（哈希）少走字符串反复替换。
- **387**：`count==1` 从左找，例子全对。能跑。`count` 每次扫全串，题短没事。

日志里两道「思路」还是空的，各补一句。

---

## 还没做完的收官件

`plan/第5周-进度追踪.md` 底部周复盘 4 问仍是题目。本人填完才能勾任务 6、更新 04 表。
