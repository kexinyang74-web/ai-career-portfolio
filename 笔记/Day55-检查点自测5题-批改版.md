# Day 55 · 第 8 周检查点自测 · 批改版

> 力扣已跳过。检查点概念题 **5✅**（第 2–5 题多摘讲解，意思对；第 1 题三个词清楚）。RAG 核心检查点通过；「每周 2 道力扣」本周不算过。

---

## 第 1 题：三个词（核心）

**批改：✅** Embedding = 文本→语义向量，不是 Chat 的 `content`。余弦比方向不是比谁更大。向量库 = 存向量并检索；Chroma 只是一种实现。本周实际用的是 `embeddings.json` 列表，不是 Chroma。

---

## 第 2 题：流水线

**批改：✅** 五步和脚本对应正确：docs → `01_split.py` → `01_embed.py` → `01_search.py` → `01_rag_chat.py`。

---

## 第 3 题：放哪 + 引用

**批改：✅** 片段进本轮 `user`；`02.md` 是「能引用到来源」那一层。

---

## 第 4 题：有无 RAG

**批改：✅** 无检索看不见笔记；有检索用 `02.md`。「资料不足」按当天 system 不算失败。

---

## 第 5 题：边界

**批改：✅** 手写切分/余弦/拼 prompt，无 LangChain。噪音块进 top-k 仍会编/混。

---

日志与周复盘已代填并标待确认。未开第 9 周。
