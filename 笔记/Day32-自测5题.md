	# Day 32 · 自测 5 题（作答模板）

> 先跑通 `01_stream.py` 再答。

## 第 1 题：两个 stream

body 里的 `"stream": true` 和 `requests.post(..., stream=True)` 各管什么？为什么要两个都开？

你的答案：


## 第 2 题：取字路径

非流式取回复是 `message.content`。流式每一小片在 JSON 的哪条路径？结束标志是什么？

你的答案：
流式每一小片在 JSON 的
 每一片 `delta.content`这样
结束长  data: [DONE]这样

## 第 3 题：拼完整回复

客户端怎样得到一整段话？为什么 `print` 要写 `end=""` 和 `flush=True`？

你的答案：


## 第 4 题：和骨架

项目一哪一个函数在干今天这件事？交互模式里用哪条命令打开流式？

你的答案：


## 第 5 题：现象

你跑 `01_stream.py` 时，字是边生成边出现，还是等很久才整段出来？完整回复大概写了什么（一句，不要贴密钥）？

你的答案：
