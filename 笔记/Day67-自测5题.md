# Day 67 · 自测 5 题（作答模板）

> `--all -k 5` 跑过再答。不要写密钥。

## 第 1 题：对照文件

k=3 和 k=5 的运行记录各在哪个文件？有没有覆盖基线？

你的答案：

k=3 基线： `projects/project-2-rag/eval/运行记录.md`（30 条，k=3）

k=5 全量： `projects/project-2-rag/eval/运行记录-k5.md`（30 条，k=5）

没有覆盖基线。 `--out` 写到了另一个文件；`运行记录.md` 开头仍是「跑了 30 条，k=3」。Day 66 还有一份抽样：`eval/运行记录-k5-`

## 第 2 题：第 24 条

全量 k=5 时第 24 条检索里还有没有 `02.md`？引用命中了没有？

你的答案：

还有 `02.md`。 全量 k=5 第 24 条检索是：`10.md`、`05.md`、`01.md`、`02.md`、`04.md`。和 Day 66 抽样一致。

引用命中了。 脚本自动是「是」；回答把 `02.md`（30 岁转行）和 `10.md`（换行业面试）列为最直接，并写了文件名。`04.md` 在检索里，主名单没把它当成转行篇。
## 第 3 题：有没有变吵

抽第 1 条（转行晚不晚）：k=5 检索多了哪几篇？回答有没有把不相关篇写进主答？

你的答案：

k=3 检索： `02.md`、`03.md`、`10.md`  
 k=5 多了： `01.md`（费曼）和 `09.md`（北京程序员一天）。`03.md`、`10.md` 本来就在。

主答没有被带跑。 仍只依据 `02.md`：25 岁还不晚、30 岁笔记里没有最终结论。其它篇只写了「涉及转行或编程，但没有直接回答晚不晚」，没有把做 App、费曼、浮生一日写进结论。

加大 k 这次是「user 更吵、回答还黏在对的篇」。不保证每条都这样，所以才要全量对照。

## 第 4 题：数字

k=5 自动引用命中是多少？和 k=3 的 27/30（自动）或 29/30（人工）比，升了还是掉了？

你的答案：

k=5 自动引用命中是 28/30 = 93%。（否的仍是 28、29；第 24 条变成「是」。）

和 k=3 比：

- 对比 自动 27/30（90%）→ 升了（多对第 24 条）
- 对比 人工 29/30（97%）→ 自动分仍低一点，因为 28、29 脚本还是因回答里出现了检索到的 md 打否；人若仍按「没有冒充库里有这篇」来记，人工引用可以到 30/30

可接受率 k=5 还没逐条重勾；第 1 条主答没被带跑，第 24 条已补上 `02.md`。默认 k 先别改，等你第 5 题拍板。

## 第 5 题：默认 k

你决定默认继续 3，还是改成 5？改了 `main.py` 没有？为什么？

你的答案：
建议默认改成 5。`main.py` 还没改，现在仍是 3。

理由：这条约 10 篇的库里，k=3 唯一双否是第 24 条漏召回；k=5 补上了 `02.md`，自动引用 27/30 → 28/30。第 1 条虽然多塞了 `01.md`/`09.md`，主答仍黏在 `02.md`，没有把无关篇写进结论。10 条向量上 k=5 成本也可接受。

继续用 3 也可以：单篇题已经 29/30，加大 k 永远更吵；「只根据某一篇」应走 `--source`，不是靠默认 k。

要落地的话说一声「把默认 k 改成 5」，我再改 `main.py`。没发话前保持 3，避免评测基线和线上默认对不上。