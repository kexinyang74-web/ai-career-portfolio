# Day 46 · 自测 5 题 · 批改版

> 对照作答模板、`demo/` 下已有 mp4、未推进 git。自测 **4✅ 1⚠️**。演示硬任务按「只录出选题」算完成。

---

## 第 1 题：录什么

**批改：✅** 只录一句出选题；一分钟录不完场 2/3。和你昨天的取舍一致。

---

## 第 2 题：画面禁忌

**批改：⚠️** `.env` 不进画面：防密钥、防被扣费，对。

「不是同一种风险」方向对，缺半句：`chat_history.json` 是 **对话隐私**（选题、口播稿），不是 API 密钥。同类是「别公开到视频/git」；不同是钱 vs 内容。Day 33 那两句 gitignore 理由，用在录屏上一样。

---

## 第 3 题：和规格书

**批改：✅** 陌生人能看出是自媒体选题助手即可。

---

## 第 4 题：和项目二

**批改：✅** 现在这条证明「会调模型出选题」；RAG 那条证明「回答带来源」。两条都留，不是用新的覆盖旧的。

---

## 第 5 题：产物在哪

**批改：✅** `projects/project-1-ai-assistant/demo`，未推 git。文件名带 README/Cursor，点开确认画面是终端出题，不是编辑器。

---

日志空格已按你的路径整理。已在项目 `.gitignore` 加上 `demo/*.mp4`，避免以后误 add。
