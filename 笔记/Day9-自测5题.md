# Day 9 · 函数与字符串处理（自测 5 题）

> 作答规则：先自己写，不许翻笔记/文档；写不出来的地方标注"没掌握"。写完后发给 Codex 批改，批改后生成批改版。

## 第 1 题：函数定义与调用（写代码）

定义一个函数 `greet(name)`，接收一个名字，返回 `"你好，{名字}！"`。调用两次，分别打印出 `你好，小明！` 和 `你好，小红！`。

作答：


## 第 2 题：return 的作用（写代码 + 一句话）

写函数 `add(a, b)` 返回两数之和，调用后把结果存进变量并打印。再用一句话说明"return 结果"和"只 print 不 return"的区别（提示：调用处能不能拿到结果继续用）。

作答：


## 第 3 题：字符串方法（写代码）

`s = "  Hello, Python  "`，分别打印：①去两端空格 ②全大写 ③把 `"Hello"` 替换成 `"Hi"` ④按 `", "` 分割成列表。

作答：


## 第 4 题：字符串切片（写代码）

`s = "python"`，用切片分别取出并打印：`yth`、`nohtyp`（反转）、`pto`（隔一个取一个）。

作答：


## 第 5 题：综合（写代码）

写函数 `count_words(text)`，用 `split()` 数出句子里的单词个数并返回。调用 `count_words("I love python")`，应打印出 `3`。

作答：

