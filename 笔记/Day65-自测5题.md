# Day 65 · 自测 5 题（作答模板）

> 至少跑过 `eval_run.py`（前 10 条也算）。不要写密钥。

## 第 1 题：跑了什么

你跑了多少条？命令是 `python eval_run.py` 还是 `--all`？结果写在哪个文件？

你的答案：

跑了 30 条。 记录里写的是「跑了 30 条，k=3」，所以命令是 `python eval_run.py --all`，不是默认只跑前 10 条的 `python eval_run.py`。

结果在： `projects/project-2-rag/eval/运行记录.md`  
 打分已填回：`projects/project-2-rag/eval/评测集.md`。
## 第 2 题：两个指标

脚本自动填的是哪一项？哪一项必须你看完回答再勾？为什么不能两项都全自动？

你的答案：

脚本自动填的是「引用命中」（按回答里有没有预期的 `02.md` 这类文件名来猜）。

必须人看完再勾的是「可接受」。 要判断有没有依据笔记、有没有胡编标题/结论、说「资料不足」算不算过。这些不能靠有没有文件名决定。

不能两项都全自动： 文件名对了，内容仍可能把 `03.md` 写进转行答案；文件名没写，内容却全对。第 28、29 条更明显——回答里出现了检索到的 md，脚本会打成引用否，人却能看出它并没有把那些篇冒充成「库里有爬虫教程」。可接受是语义对错，脚本没有这份判断。
## 第 3 题：前几条现象

就你跑到的条目：引用命中(自动)大概几条「是」？有没有预期对、但回答没写文件名的？

你的答案：

自动「是」是 27/30（90%）。 记录底部写了这句。3 条自动「否」是 24、28、29。

没有「预期要对某一篇、回答也对、却一个文件名都没写」这种情况。 前 23 条以及 25–27、30 都写了对应 md。

第 24 条不是没写文件名，而是写了 `10.md / 05.md / 01.md`，漏了预期的 `02.md`。第 28、29 条预期是「无」，回答里仍出现了检索到的 md，所以脚本打否；内容其实在说这些篇里没有爬虫/播放量。
## 第 4 题：无来源题

如果已经跑到 28/29：它有没有编一个 md 文件名？可接受你怎么打？

你的答案：

没有编一个假的 md 来冒充「库里有这篇」。 28、29 列出的 `06.md`、`01.md`、`04.md`、`09.md` 都是检索真拿到的块，并且写明这些篇里没有爬播放量、也没有精确播放/涨粉。

可接受我打的是「是」。 该说没有就说了没有，没有编教程或假数据。

引用命中：脚本因为看到 md 文件名打了「否」；人判定它们没有把这些篇当成答案来源，所以评测表里引用也记了「是」。若你坚持「预期为无就一个文件名都不能出现」，可以把这两条引用改回否，可接受仍建议保持是。
## 第 5 题：还没做的

今天有没有改 `-k` 或重切文档？可接受率、引用命中率你算出来了吗（没有填表也可以说还没算）？

你的答案：

没有改 `-k`，也没有重切文档。 还是默认 k=3，库仍是 Day 57 那份 Chroma。

算出来了，也填进表了：

- 可接受率：29/30 = 97%（人工，第 24 条否）
- 引用命中率：29/30 = 97%（人工，第 24 条否）
- 脚本自动引用命中曾是 27/30 = 90%（把 28、29 也打成否）

都高于目标 70%。调 k 或重切是 Day 66 的事。