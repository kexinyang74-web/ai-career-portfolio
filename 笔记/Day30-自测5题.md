# Day 30 · 自测 5 题（作答模板）

> 作答方式：直接在每题下面写答案，答完发给我批改。先跑通 `01_single_turn.py` 再答，会好写很多。

## 第 1 题：为什么是 POST

调 Chat Completions 为什么一般用 POST 而不是 GET？body 里最不能缺的两个字段是什么？

你的答案：


## 第 2 题：鉴权

`Authorization` 头要写成什么格式？密钥为什么不能写进 `01_single_turn.py` 源码？

你的答案：


## 第 3 题：取回复

非流式成功时，模型说的话在 JSON 的哪条路径？请写成 `data[...]` 这种下标写法。

你的答案：


## 第 4 题：401 和超时

收到 401 时你先检查什么？`timeout=120` 是在防什么？和 Day 19 的 SSL 报错该先试哪招？

你的答案：


## 第 5 题：usage

`prompt_tokens` 和 `completion_tokens` 分别对应谁产生的 token？你这次运行打印出来的三个数字是多少？（没有跑通就写「未跑通」和原因。）

你的答案：
