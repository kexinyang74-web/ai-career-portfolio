# Day 40 · 自测 5 题 · 批改版

> 对照作答模板与当前 `system_prompt.md`（文首已加 `v1（2026-09-05）零改动`，正文未改）。第 1–4 题为用户原文；**第 5 题及学习日志思考字段为代写，待你确认**。自测按用户原作答计 **3✅ 1⚠️**；第 5 题不计入你的分数。

---

## 第 1 题：四件套落点

**批改：✅** 角色 / 任务+默认格式 / 边界+不编造，大格对。

补两刀：人设那两段引号也是 **角色**（你只标了标题+第一句）；「不编造播放量」写在「你怎么干活」里，但仍是 **约束**，你标对了位置可以不跟标题走。

---

## 第 2 题：为什么在 system

**批改：✅** 不用每次重复。再钉一句：改 `system_prompt.md` 一次，之后每轮 `system` 都带着，人设不会飘。

---

## 第 3 题：默认 vs 用户指定

**批改：⚠️** 「听用户的」是你的产品意图；「没写清」也属实。

「但是他会识别出来」说满了。Day 39：B 写了 5 条一行，仍补了「为什么适合你」；A 没写条数，直接走 10+3。模型 **有时** 会让路，文件里 **没有**「用户指定了条数/格式就不要 10+3、不要再补一句」。零改动 = 你接受这个不确定性。

---

## 第 4 题：少放什么

**批改：✅** 后半句分工对：规则在 system，某一轮的样子在 user（C）。

前半句「低质量例子会带坏」也对，但不是这题主因。主因是：例子放进 system 后 **每一轮都在**，写口播稿时也会去抄那条选题样例，和默认 10+3 抢形状。质量差是次要风险。

---

## 第 5 题：设计决策（代写，待确认）

**批改：未作答 → 代写如下，不算你写的。**

挑「用户丢来赛道时默认 10 个选题 + 3 个开头」这一段：改前就是糊指令时的默认产量。不改，是因为 v1 仍要服务「帮我想几个选题」这种半句话，足量比精简优先；用户要 3 条时靠这一轮 user 去压（Day 39 的 B/C）。文首只加版本标记，等于正式收成 v1，不是假装没做过 Day 40。

若你不认这句，删掉改成你自己的「为什么不删第 11 行再补一句」。

---

日志三格亦为代填，见 `学习日志/2026-09-05-Day40-学习日志.md`，文内已标明。
