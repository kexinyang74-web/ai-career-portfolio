# Day 11 · 文件读写（txt/JSON/CSV）自测 5 题

> 作答规则：先自己写，不许翻笔记/文档；写不出来的地方标注"没掌握"。写完后发给 Claude 批改，批改后生成批改版。

## 第 1 题：读文件（写代码）

有一个文件 `names.txt`（内容为 3 行名字）。用 `with open` 把它的内容**逐行**打印出来（每行一个名字，不要有多余的空白行）。请写出完整代码。

作答：
```python
with open("name.txt","r",encoding="utf-8") as f:
	for line in f:
        print(line.strip())

```
## 第 2 题：json 四兄弟（选择填空）

下面 4 个函数，分别在哪个场景用？填空（填 ①dumps ②loads ③dump ④load）：

- （1 ）把字典变成字符串：`json.___(user)` → `'{"name": "小明"}'`
- （4 ）把字符串变回字典：`json.___('{"name": "小明"}')` → 字典
- （2 ）把字典直接写进文件：`json.___(user, f)`
- （ 3）从文件直接读成字典：`data = json.___(f)`

一句话说：怎么记住带 s 和不带 s 的区别？

## 第 3 题：写 JSON 文件（写代码）

把下面的字典写进 `student.json`，要求：中文正常显示（不出现 `\u`）、文件里缩进换行（人看得懂）：

```python
student = {"name": "小红", "age": 18, "hobby": ["读书", "跑步"]}
```

请写出完整代码。

作答：

## 第 4 题：读 CSV（写代码）

有一个 `scores.csv`（第一行是表头"姓名,语文,数学"，后面是成绩数据）。用 `csv.reader` 读出**每一行**并打印，同时**跳过第一行表头**（只打印数据行）。请写出完整代码。

作答：

## 第 5 题：综合（写代码 + 一句话）

写一个函数 `save_user(user_dict, filename)`：把传入的字典用 JSON 格式写进文件（带 utf-8、中文正常、缩进）。然后调用它，把 `{"name": "小明", "age": 20}` 存成 `user.json`，再用 `json.load` 读回来打印 `name` 字段。

最后用一句话回答：**为什么写文件要用 `with open` 而不是 `open` + 手动 `close`？**

作答：
