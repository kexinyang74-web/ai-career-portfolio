# Day 40 · 自测 5 题（作答模板）

> 对照当前 `system_prompt.md` 作答。零改动也算 v1，但第 3、5 题必须写清「为什么不改」。跑过一次糊 user 再答。

## 第 1 题：四件套落点

打开你现在的 `system_prompt.md`，各用几个字标出：哪一段是角色、任务、约束、格式？

你的答案：
`# 系统提示词：B站选题与脚本助手你是面向 B站 创作者的**选题与脚本助手**。`是 角色；「你怎么干活」是 任务 + 默认格式；「边界」和不编造是 约束。

## 第 2 题：为什么在 system

人设和「只帮 B站 创作」为什么放在 system，而不是每次写进 user？

你的答案：
为了不用每次输入重复信息

## 第 3 题：默认 vs 用户指定

用户写了「只要 3 条、每条一行」时，system 里的「默认 10+3」按你的 v1 应该听谁的？你文件里有没有写清？

你的答案：
听用户的
没写清但是他会识别出来
## 第 4 题：少放什么

为什么 v1 不建议在 system 里塞一整条 few-shot 长例子？和 Day 39 的 C 版怎么分工？

你的答案：
因为不确定这个例子符不符合我的要求，低质量的例子会让生成的效果也低质量
规则放 system，某一轮的样子放 user（C）。 某一轮的例子只需要在某一轮写入就行。
## 第 5 题：设计决策（检查点核心）

你这次改动里，挑 **一段** 写出：改前它在干什么、你为什么改/为什么不改。

你的答案：
（代写，待确认）默认 10+3 这一段改前是糊指令时的产量；我不改正文，是因为半句「帮我想选题」仍要够用的清单，精简交给某一轮 user。文首只加了 v1 标记。
