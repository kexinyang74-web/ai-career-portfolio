# Day 41 · 第 6 周检查点自测 · 批改版

> 对照作答模板、`system_prompt.md`、day41 力扣。用户原作答 **3✅ 2⚠️**；第 4 题根对象为代补后第 4 题改判 ✅，合计 **4✅ 1⚠️**（第 3 题仍 ⚠️）。周复盘/日志空格已代填，**待你确认**。第 6 周按记录收官（推送仍等你发话，勿推 `.env`）。

---

## 第 1 题：为什么这么写（核心）

**批改：✅** 挑 10+3 当 **任务**（也带默认格式）对；糊 user 走 10+3 对；副作用用 Day 39 的 B「结尾再补为什么适合」也对。

补一句：10+3 同时占 **任务 + 默认格式**；「再补一句为什么适合」是另一条，来自「你怎么干活」第一句，不是 10+3 自己长出来的。

---

## 第 2 题：四件套

**批改：✅** 四格半句和落点与 Day 40 一致，过。

---

## 第 3 题：few-shot 和 CoT

**批改：⚠️** 后两问对：只要清单就关 CoT；例子放某一轮 user。

前半偏了（和 Day 37 同一处）：

- few-shot 主要是 **给一条样子让模型抄形状**，不是泛泛「表述模糊」（模糊更先靠四件套把任务/格式写清）。
- CoT 是 **先走步骤再给结果**，不是「让回答变准」。步骤写错，清单照样虚。

---

## 第 4 题：结构化输出

**批改：✅**（第二问为代补）messages 里要出现 JSON、`main.py` **没有**传 `response_format`，是你写的。根必须是对象、列表放进 `items`，是代写补上的，待确认。

---

## 第 5 题：三版对比 + v1

**批改：✅** 固定变量、算交卷、交的是设计决策。三句话够用。

---

## 力扣

- **389** `findTheDifference`（异或）和 `findTheDifference2`（Counter）本地 `e` / `y` 通过。
- **`findTheDifference3`**：`sorted` 得到的是 **list**，不能 `list - list`。差集要用 `Counter` 相减或先变成 `Counter`/`set`（`set` 会丢掉重复字母，这题多一个字符用 Counter 更稳）。网站交第 1 或第 2 个方法即可。
- **409**：`has_odd = False` 已补；`abccccdd`→7、`a`→1 通过。

日志思路与周复盘已代填，待确认。

---

## 周复盘

已写入日志和 `plan/第6周-进度追踪.md`（代填待确认）。`04-进度追踪表` 第 6 周行已更新。
