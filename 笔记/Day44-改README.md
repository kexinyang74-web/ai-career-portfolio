# Day 44 改 README（第 7 周第 2 天）

> 规格书：README 含简介、运行方法、**示例对话**、架构说明。定位句（文件第 3 行「面向自媒体创作者（B站）」）**不要改**。示例对话和单次提问 **你自己改成选题**，我不代写那两句 user。不要把真实密钥写进 README。

文件：`projects/project-1-ai-assistant/README.md`

## 今天必须改的两处（电商残留）

| 现在（错位） | 改成 |
|---|---|
| `python main.py "帮我写一段 XX 行业的新品推广文案"` | 一句 **选题/脚本** 提问（你编，可参考你 Day 43 真问过的） |
| `你 > 帮我分析一下这个月销量下滑可能的原因` | 同上，交互里的「你 >」也要是选题 |

`AI >` 可以仍写「（模型回答）」或贴你跑过的一两行摘要，不要贴整篇 10+3 小作文，也不要密钥。

## 今天必须补的一节：架构

在「目录结构」附近加一个小节（标题自定，如「架构」），用你自己的话写清这三句就够：

1. `system_prompt.md` 启动时读进 **system** 消息
2. 交互时 user / assistant 追加进内存 **`history`**，每次请求整份带上
3. `/save` 写入 `chat_history.json`；`/new` 只清内存，**不读**这个文件当下一句的上下文

可顺手在目录树里补一行 `chat_history.json`（本地生成、不进 git）。

## 明确不做

- 不改第一行定位、不录视频、不写 STAR、不加 API 重试（你已定本周不做）
- 不提交 `.env`

## 做完

自己通读一遍：陌生人只看 README，能不能在本机跑起来、会不会以为这是电商助手。然后答自测、填日志。改完可以把 README 发我批（对照规格四书：简介 / 运行 / 示例 / 架构）。
