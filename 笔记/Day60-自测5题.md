# Day 60 · 自测 5 题（作答模板）

> 脚本 1/2 检索、2/2 问答都跑过再答。不要写密钥。

## 第 1 题：对上号

VectorStore 对应你第 8 周哪一步？教材里的 RetrievalQA 对应哪一步？今天有没有重新 Loader / 切那 10 篇？

你的答案：
VectorStore = 第 8 周的 检索：问句变成向量，在库里按距离取 top-k。对应 `day53/01_search.py`，以及 Day 57 的 Chroma `query`。今天脚本里是 1/2 的 `similarity_search_with_score`。RetrievalQA = 第 8 周的 RAG 问答：把检索到的片段塞进本轮 `user`，再调 Chat。对应 `day54/01_rag_chat.py`。今天是 2/2（检索 → 拼进 user → Chat）。它不是第四种切法，也不是另一个向量库。

今天没有重新 Loader，也没有再切那 10 篇。

## 第 2 题：片段放哪

检索到的正文进了 `system` 还是本轮 `user`？Chat 会不会自己去读 `chroma_db` 文件夹？

你的答案：
正文进了本轮 `user`，不是 `system`。

脚本里检索片段拼进 `user_text`，再作为 `HumanMessage` 发出去（LangChain 里这就是 user）。`system` 只放规则：有资料就按资料答、写来源、不足就说不足。
Chat 不会自己去读 `chroma_db`。 那是你电脑上的向量库。DeepSeek 只能看见这次请求里的 `messages`。没人把片段塞进去，它就看不见 `02.md`。和 Day 54 同一件事。
## 第 3 题：你看到的检索

1/2 的 top-3 是哪几篇？score 大概多少？和 Day 58 不过滤那次像不像？

你的答案：


## 第 4 题：问答

2/2 有没有点到 `02.md` 或你的钩子？如果模型把 `03.md` 的内容写进「转行晚不晚」，那是检索出了问题还是 Chat 混进了噪音？

你的答案：


## 第 5 题：还没做的

今天有没有 `where` 过滤？有没有建 `projects/project-2-rag/`？四个词现在缺哪一个？

你的答案：
