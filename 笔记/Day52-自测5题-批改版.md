# Day 52 · 自测 5 题 · 批改版

> 对照作答模板与终端：10 块、维度 1024、百炼兼容模式。自测 **3✅ 2⚠️**。作答模板保留。勿把 `.env` 写入笔记。

---

## 第 1 题：两条路

**批改：✅** 路径、`messages` vs `input`、回来是字还是浮点数，表里都对。Chat 用 DeepSeek 的 BASE，Embedding 用百炼的 `.../compatible-mode/v1`，两套 BASE 不能混。

---

## 第 2 题：返回值

**批改：✅** 浮点数列表；维度 = 列表长度。不贴完整向量：读不动、核对只看块数/维度/前几位即可。你这次维度是 **1024**（可写进日志，不必贴一千个数）。

---

## 第 3 题：模型名

**批改：⚠️** 结论「不能」对。你写「deepseek 不包含 embedding 功能」对 **DeepSeek 官方 Chat 接口**成立。补一句：`EMBEDDING_MODEL` 必须是**向量模型名**（如 `text-embedding-v3`），就算某家同时卖 Chat 和 Embedding，也不能把对话模型名填进这个字段。

---

## 第 4 题：短块

**批改：✅** 标题块没有可检索内容；跳过避免浪费调用、避免进 top-k 当噪音。和昨天每篇 2 块、第 1 块只有标题对得上。40 只是门槛，不是最优切分理论。

---

## 第 5 题：还没做的

**批改：⚠️** 「没有」对。只说「后续安排」偏空。今天验收的是 **切块 → 向量**；余弦、top-k、Chat 是 Day 53–54。流水线还没跑通，所以检查点「约 10 篇能问答」还不能勾。

---

日志「今天做了什么」已有：百炼、10 块、1024。空着的原理/问题已按你真实操作补了两三句，**待你确认**。
