# Day 43 · 自测 5 题（作答模板）

> 亲手跑过交互命令再答。不要写密钥。

## 第 1 题：多轮

第二句提问时，模型为什么还能接到第一句的选题？`/new` 之后为什么接不上了？

你的答案：
它会读取上下文对话。给你它有记忆的假象。
因为把上下文重置了。所以就接不上了。但是它会胡编。需要在sys里+限制让他别胡编。

## 第 2 题：流式

`/stream` 切换的是哪一个变量？流式时文字是从 JSON 的哪条路径一块块拼起来的？（能写到 `delta` 即可）

你的答案：


## 第 3 题：保存与 git

`/save` 写到哪个文件？这个文件为什么通常不进 git？和 `.env` 不进 git 是同一原因吗？

你的答案：
chat_history
因为有对话历史，属于个人隐私所以不进git
不是同一原因，.env不进git是防止密钥泄露。造成资金损失

## 第 4 题：错误处理 vs 重试

现在超时或 API 失败后程序会怎样？规格书里的「错误重试」你今天决定本周做还是不做？一句话为什么。

你的答案：


## 第 5 题：验收还缺什么

对照规格书，README 里「示例对话」现在还是不是选题场景？今天为什么先不改 README？

你的答案：
