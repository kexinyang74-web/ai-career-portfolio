# Day 38 · 自测 5 题 · 批改版

> 对照原文和日志。列表字段、JSON 解析均已跑通。**5/5**（第 1 题有一句说满了，不影响过关）。

---

## 第 1 题：为什么要字段

**批改：✅** 相关格子是 **输出格式**。补一句：字段齐只保证**能验收、能解析**，不保证选题更好（你第 3 题末句已经写对了）。「一定更有可用性」稍满。

---

## 第 2 题：json_object

**批改：✅** messages 里要出现 JSON 指令，否则可能空转/报错。项目一 `main.py` **没有**传 `response_format`。

---

## 第 3 题：根必须是对象

**批改：✅** `json_object` 要的是一个对象；用 `items` 包数组，后面 `obj["items"]` 取列表。包装不管内容好不好。

---

## 第 4 题：解析失败先看哪

**批改：✅** 先看原文有没有围栏、前后废话、截断。开了 json_object 仍夹说明，再查模式有没有生效。

---

## 第 5 题：跑通了吗

**批改：✅** 日志里列表已是「标题/钩子/人设」两条；`01_json_mode.py` 解析成功，顶层键 `items`，三条字段都在。

---

日志原理与第 2 题同义，可定稿。
