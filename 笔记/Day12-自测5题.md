# Day 12 · 异常处理与 logging 自测 5 题

> 作答规则：先自己写，不许翻笔记/文档；写不出来的地方标注"没掌握"。写完后发给 Claude 批改，批改后生成批改版。

## 第 1 题：捕获文件不存在（写代码）

有一个文件 `data.txt`（**可能不存在**）。用 `try/except` 读它：文件存在就打印内容；不存在就打印友好提示「⚠️ 文件不存在」，并且**程序不要翻车**。请写出完整代码。

作答：

## 第 2 题：try/except/else/finally 执行顺序（填空）

看下面的代码，**写出每种情况下的输出**：

```python
try:
    number = int("100")      # 情况 A：这里能转成功
    # number = int("abc")    # 情况 B：这里会转失败（ValueError）
except ValueError:
    print("① 转换失败")
else:
    print("② 转换成功")
finally:
    print("③ 收尾")
```

- 情况 A（能转成功）的输出是：______
- 情况 B（转失败）的输出是：______

一句话总结规律：else 在______时执行，finally 在______时执行。

作答：

## 第 3 题：防除零（写代码）

写一个函数 `safe_divide(a, b)`：两数相除。如果 `b` 为 0，主动抛出 `ValueError`，提示「除数不能为 0」；否则正常返回结果。调用时用 `try/except` 接住错误并打印提示。请写出完整代码。

作答：

## 第 4 题：print vs logging（简答）

1. logging 有 5 个级别，按从低到高写出来：______
2. 至少说出 `print` 和 `logging` 的 3 个区别（用表格或分点）：
3. 日志要**写进文件**，在 `basicConfig` 里加哪个参数？______

作答：

## 第 5 题：综合（写代码）

把一个「读 JSON 文件」的脚本加上防护和日志：

- 用 `try/except` 接住两种错：文件不存在（FileNotFoundError）、JSON 内容损坏（json.JSONDecodeError）
- 用 `logging` 记录：程序开始（INFO）、读成功（INFO）、出错（ERROR）
- 读完打印 `data["name"]`

请写出完整代码。

作答：
