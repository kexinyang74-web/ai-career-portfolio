# 20. 有效的括号
# 给定一个只包括 '('，')'，'{'，'}'，'['，']' 的字符串 s，判断字符串是否有效。
# 有效字符串需满足：
#   1. 左括号必须用相同类型的右括号闭合
#   2. 左括号必须以正确的顺序闭合

# 示例 1：
# 输入：s = "()"        → True
# 示例 2：
# 输入：s = "()[]{}"    → True
# 示例 3：
# 输入：s = "(]"        → False
# 示例 4：
# 输入：s = "([)]"      → False  （括号顺序错误：先 [ 后 )，交叉了）
# 示例 5：
# 输入：s = "{[]}"      → True

class Solution:
    def isValid(self, s: str) -> bool:
        # 你的代码写在这里
        pass
