# Day 54 一般不单放密钥。脚本会先读项目一 .env，再读 day52/.env。
# Embedding：DASHSCOPE_API_KEY + EMBEDDING_BASE_URL + EMBEDDING_MODEL（day52）
# Chat：DEEPSEEK_API_KEY；对话 BASE 默认 https://api.deepseek.com，模型默认 deepseek-chat
# 不要把 .env 提交或贴到聊天。
