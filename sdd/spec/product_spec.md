# 产品规格（product spec）

> 复制本文件为你的功能规格，按项目实际情况填写。参照笔记中的"待办 API 规格示例"。

## 1. 场景化信息

- **背景**：（为什么做、解决什么问题）
- **用户画像**：（谁用、核心用法）
- **使用场景**：（在什么情况下、输入输出是什么）

## 2. 数据模型

```text
实体：<名称>
- 字段 1：类型、必填性、约束（示例：id: int，服务端自增，只读）
- 字段 2：类型、默认值、边界（示例：title: str，去空白后长度 1-100）
```

## 3. 接口设计

| 方法 | 路径 | 请求体 | 成功返回 | 失败返回 |
|------|------|--------|----------|----------|
| POST | /xxx | {...} | 201 + 完整对象 | 422 校验失败 |
| GET  | /xxx | - | 200 + 列表 | 404 不存在 |

## 4. 校验规则与边界条件

- （示例：空标题 → 422；不存在的 id → 404；重复标记 → 幂等 200）

## 5. 明确不做的事（排除范围）

- （示例：删除、编辑标题、分页、持久化、鉴权均不在本期范围）

## 6. 验证方法

- 静态校验：`python -m pyright`
- 动态测试：`python -m pytest`（用例与规格逐条对应）
- 人工复检：验收标准是否完整、业务逻辑是否正确
