# 2026-08-29 Day 20 学习日志

> Day 20（第 3 周第 6 天，收官日）：小脚本 2——公开 API 存 CSV + 检查点自测 + 周复盘。思考性字段由本人填写（或委托 Claude 代填）。**"遇到的问题"和"周复盘"为 Claude 代填，待本人确认。**

## 今天做了什么

- 小脚本 2（projects/python-practice/day20/）：
  - 01_test_api.py：调通汇率 API（open.er-api.com，166 种货币），打印了 JPY/EUR 汇率并确认 `data["rates"]` 是字典
  - 02_parse.py：解析出想要的币种（字典 → 一行一条）
  - 03_save_csv.py：存 CSV（utf-8-sig 防乱码）
  - 04_main.py：完整版（函数拆分 + 异常全家桶）
  - README.md + 汇率.csv（生成）
- **LeetCode 策略调整**：用户决定由"每天 2 道"改为"每周 2 道"，已同步全部计划文件（01-24周执行计划/资源清单/第 3 周定稿与进度/当前状态）
- 检查点自测 5 问 + 周复盘（见下文）

## LeetCode 思路（每道一句，本人填写）

- 今日无新题：**力扣策略调整日**（2026-08-29 起每周 2 道），累计已完成 22 道 ✅ 超额

## 检查点自测（5 问，第 3 周验收，每题一句话回答）

**1. 画出 04_main.py 的模块结构图（main 调用了哪几个函数？各干什么？）**

```
main()
 ├── fetch_rates()  抓数据（请求 API）
 ├── build_rows()   解析（字典 → 行列表）
 └── save_csv()     存文件（CSV）
```
> **批注（Claude）**：结构图画对了，与 README 一致 ✅——这就是检查点 1 的答案。

**2. 为什么要拆函数/模块？（说出至少 2 个好处）**
因为更好定位错误，方便测试，复用性更强
> **批注（Claude）**：三个好处全说对 ✅（好定位 / 好复用 / 好测试）。

**3. 用 class 写一个带属性和方法的类，并说出 self 是什么**

```python
class 奶茶店:
    def 奶茶种类(self, 珍珠奶茶: str) -> bool:
        return 珍珠奶茶 in self.菜单
```
self 就是自己本身
> **批注（Claude）**："self 就是自己本身"理解准确 ✅。补充两点：① 代码里**全角括号（）、全角冒号：**是语法错误（Day 16 踩过的坑），上面已改为半角；② 类里方法调用别的方法要写 `self.方法名()`——Day 19 力扣 70 报 NameError 就是因为忘了 self。

**4. requests 调通公开 API + 异常处理 + 存 CSV 三样都完成了吗？**
完成了
> **批注（Claude）**：✅ 就是今天的小脚本 2（04_main.py 三样全含）。

**5. LeetCode 每周 2 道达成了吗？（累计完成几道？）**
完成
> **批注（Claude）**：✅ 累计 22 道，远超每周 2 道的要求。

## 今天写的代码 / 文档

- projects/python-practice/day20/01_test_api.py ~ 04_main.py + README.md + 汇率.csv
- 笔记/Day20-小脚本2.md（手把手指南）

## 一个我能讲清楚的原理（本人填写）

（例如：解析是什么意思？utf-8-sig 是干嘛的？为什么要拆函数？异常全家桶怎么帮上忙的？）
1. 解析就是把字典转成一行一条。把数据提取出来相当于
> **批注（Claude）**：理解正确 ✅。"一行一条"正是 CSV 的格式，也是今天 `build_rows` 干的事。
2. 中文显示的，没有它中文会出乱码
> **批注（Claude）**：对，但建议记住**名字：utf-8-sig**（带 BOM 的 UTF-8，Excel 靠它识别编码）。以后写 CSV 给 Excel 用，一律用它。
3. 拆函数有很多好处，比如出现问题好定位，可复用性更强。测试起来更方便
> **批注（Claude）**：✅ 三好处齐了。
4. 异常全家桶就是兜底吧，不会让脚本崩溃，出现错误也有输出。
> **批注（Claude）**：形象！"兜底"就是"④ 其他错误"分支的意义——报错总比崩溃好。

## 遇到的问题（本人填写）

（Claude 依据真实记录代填，待本人确认）
1. **04_main.py 报 SyntaxError: unterminated string literal**：docstring 里写了英文直引号 `"`（`整理成"一行一条"`），和 `"""` 同字符导致字符串提前结束。改成「」解决——**字符串内容里别放英文引号**
2. **汇率.csv 生成在了仓库根目录**而不是 day20 文件夹：相对路径的基准是"运行命令时终端所在目录"，不是脚本位置。以后想控制文件落点，先 `cd` 到目标文件夹再运行

## 遗留问题（本人填写）

- [ ] （有没搞懂的写这里，没有就留空）

## 明日计划（Day 21）

- 休息日；第 4 周预告：工程化（虚拟环境/依赖管理/测试基础 + 小脚本 3 CSV/Excel 清洗）
