# 2026-09-10 Day 57 学习日志

> Day 57：Chroma 持久化。日志代填，标待确认。不要写密钥。

## 今天做了什么

- 装 `chromadb`，跑 `01_chroma.py`：把 `embeddings.json` 写入 `day57/chroma_db/`，问句 Embedding 后 query。
- 第二次应跳过写入（持久化）。无 Chat、无 LangChain。
> **待确认**

## 一个我能讲清楚的原理（本人填写）

1. 持久化 = 向量在磁盘目录里，进程结束不丢。查询用 Chroma 的 distance（越小越近），不是自己写余弦。
> **待确认**

## 遇到的问题（本人填写）

1. distance 和上周余弦方向相反，不能拿 0.84 那套直觉套在 distance 上。
> **待确认**

## 遗留问题（本人填写）

- [x] 元数据过滤、rerank 概念（Day 58 已做）

## 明日计划（Day 58）

- 元数据过滤；top-k 与 rerank 概念
