# 2026-09-10 Day 58 学习日志

> Day 58：元数据过滤 + rerank 概念。日志代填，标待确认。不要写密钥。

## 今天做了什么

- 在 `day58` 跑通 `python 01_filter.py`（exit 0）：读 `day57/chroma_db/`，集合 10 条。问句「30岁转行学编程晚不晚？」
- 不过滤 top-3：`02.md` 0.1532 / `03.md` 0.4463 / `10.md` 0.4649
- `where source=02.md` 后只剩 `02.md` 一块（0.1532）。无 Chat、无 LangChain、无 rerank 代码。
> **待确认**

## 一个我能讲清楚的原理（本人填写）

1. `source` 是切块旁的标签，不是向量里的数。过滤先按标签砍范围，再比 distance；rerank 是先召回再改名次。
> **待确认**

## 遇到的问题（本人填写）

1. 不过滤时第 2、3 名是 `03.md`/`10.md`（噪音）；过滤后凑不满 top-3，因为符合 `02.md` 的往往只有一块。
> **待确认**

## 遗留问题（本人填写）

- [ ] Day 59：LangChain Loader / TextSplitter

## 明日计划（Day 59）

- LangChain Loader / TextSplitter，对照 `01_split.py`
