# 2026-09-05 Day 43 学习日志

> Day 43（第 7 周第 1 天）：CLI 对照规格书。思考性字段本人填写。不要写密钥。

## 今天做了什么

- CLI 验收：交互里跑过出选题、追问第 5 个、`/stream`、`/save`、`/new` 后再问「第5个选题号」
- 自测 5 题已答（批改 4✅ 1⚠️）

## 一个我能讲清楚的原理（本人填写）

（整理自「遇到的问题」，待你确认）同一会话第二句能接上，是因为 `history` 里真有上一轮。`/new` 把 `history` 重置为只剩 system，API 看不到旧清单；这时再说「第 5 个」，模型仍可能口头假装记得。要限制就改人设里「按上一轮修改」那一句，不是 `/new` 没生效。

## 遇到的问题（本人填写）

今天发现/new后ai还会假装记得上轮对话再瞎编。需要在提示词里面新加限制词。比如输/new命令后，不要假装记得上轮对话这种

> **批注：** 不必写「输了 /new」——模型看不到这条命令（`/new` 不进 `messages`）。限制应写成：历史里没有清单时不要假装有上一轮。

## 遗留问题（本人填写）

- [x] 本周是否给 API 失败加有限次重试：本周不做（对照自测第 4 题，先保演示）
- [ ] 是否改 `system_prompt.md` 里「多轮时根据上一轮修改」那一句（你点头再说）

## 明日计划（Day 44）

- 改 README：示例改成选题；补架构；定位句不动
