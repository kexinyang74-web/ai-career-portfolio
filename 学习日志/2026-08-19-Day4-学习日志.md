# 2026-08-19 Day 4 学习日志

> Day 4：Python 环境搭建 + .env 密钥管理。2026-08-20 上午完成自测补漏后定稿。

## 今天做了什么

- 确认本机环境：Python 3.13.5（满足 3.12+）、pip 25.1；VS Code 已安装（`C:\Users\Administrator\AppData\Local\Programs\Microsoft VS Code`，v1.131.0），也可用 Cursor
- 在项目一骨架（projects/project-1-ai-assistant）创建 `.venv`，安装 requests / python-dotenv，`python main.py --help` 验证可运行
- 学会 venv 心智模型（项目独立小房间）、`pip install -r requirements.txt`、`pip list` / `pip freeze`
- 创建 `.env`（占位密钥），学会 `load_dotenv()` + `os.getenv()` 读取；用"只显示长度和前几位"的方式验证密钥已加载
- 用 `git check-ignore` / `git status` 验证 `.env` 和 `.venv` 不会被提交
- 教学笔记：笔记/Day4-Python环境与密钥管理.md

## 遇到的问题与解决

- PowerShell 控制台中文显示乱码：编码问题（不影响运行结果），后续命令可设 UTF-8 输出
- 如果激活 venv 报"禁止运行脚本"：先执行 `Set-ExecutionPolicy -Scope Process Bypass`
- 暂无其他卡点

## 今天写的代码 / 文档

- projects/project-1-ai-assistant/.env（占位密钥，本地文件，未提交）
- projects/project-1-ai-assistant/.venv（本地虚拟环境，未提交）
- projects/project-1-ai-assistant/test_env.py（自测补漏验证脚本，未提交）
- 笔记/Day4-Python环境与密钥管理.md

## 一个我能讲清楚的原理

- venv = 项目专属独立小房间，装依赖互不干扰
- 密钥进 .env、.env 进 .gitignore、代码只读环境变量——真实密钥永远只在本地

## 还没搞懂的部分

- [ ] venv 激活命令：`.\.venv\Scripts\Activate.ps1`（自测第 2 题把"路径"当成了"命令"）
- [ ] `.env` 格式：等号两边无空格、值不加引号；"忽略 ≠ 追踪"（自测第 3 题 C/D 答反了）
- [ ] 密钥已提交 Git 后的完整补救：`git rm --cached .env` + 去平台换新密钥（自测第 5 题只答对了一半）

> 三条都来自 Day4 自测批改版（[[Day4-自测5题-批改版]]），是真实作答暴露的缺口。Day 5 开始前花 10 分钟重看一遍批改版"核心要点"即可清账。

## 今日验证记录（2026-08-20 上午）

- ✅ `python test_env.py` 亲手跑通 → 输出"密钥长度： 12"（.env 里是 12 字符占位符；以后换真实密钥，代码不用改）
- ✅ `git status` 输出中没有 `.env`；`git check-ignore .env` 输出 `.env`，确认已被忽略

## 明日计划（Day 5）

- 定"作品三件套"目标：参照 [[03-项目三件套规格书]] 选行业主题、写三个项目方向（模板：plan/05-作品三件套目标.md）
- 完成 Day5 自测 5 题（笔记/Day5-自测5题.md）
- 写第一篇学习笔记（Day5 学习日志）并提交推送到 GitHub
