# 2026-09-12 Day 59 学习日志

> Day 59：LangChain Loader / TextSplitter。日志代填，标待确认。不要写密钥。

## 今天做了什么

- `pip install -r requirements.txt` 后跑通 `python 01_split_lc.py`（exit 0）。有 `langchain-community` sunset 警告，未改包。
- 对照：10 篇 / 10 个 Document；手写按空行 20 块；LC 按空行合并 10 块；定长 80/20 为 83 块。无 Embedding、无 Chroma、无 Chat。
> **待确认**

## 一个我能讲清楚的原理（本人填写）

1. Loader = 读文件成 Document（带 `source` 标签）；TextSplitter = 切块。同一批文档，刀法不同块数不同（20 / 10 / 83）。
> **待确认**

## 遇到的问题（本人填写）

1. 起初把今天当成比 Loader vs Splitter，后面对上「库版的读文件→切块」。LC 按空行是 10 不是 20，因为 `chunk_size=2000` 会把短段落黏回一篇一块。
> **待确认**

## 遗留问题（本人填写）

- [ ] Day 60：VectorStore / RetrievalQA 对上手写检索和 RAG 聊天

## 明日计划（Day 60）

- VectorStore / RetrievalQA，对照 `01_search.py` + `01_rag_chat.py`
