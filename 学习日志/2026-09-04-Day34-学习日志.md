# 2026-09-04 Day 34 学习日志

> Day 34（第 5 周收官）：人设 + 力扣 2 道 + 检查点自测 + 周复盘。思考性字段由本人填写。密钥不要写进本文件。

## 今天做了什么

- 修改项目人设
- 做两道力扣题

## LeetCode 思路（每道一句，本人填写）

- （383）：主要解决难点在于magazine = magazine.replace(i, "", 1) # 将magazine中的i字符替换为空字符串，并只替换第一个出现的字符。magazine 中每个字符只能用一次。因为每个字符只能出现一次。
- （387）：if s.count(s[i]) == 1:关键点在于这个。这句代码经常用于查找“第一个不重复的字符”：

## 今天写的代码 / 文档

- `projects/project-1-ai-assistant/system_prompt.md`、`README.md`
- `projects/python-practice/day34/leetcode_383.py`、`leetcode_387.py`

## 一个我能讲清楚的原理（本人填写）


> **批注（Claude）**：383 思路对在 `replace(..., 1)`——每用掉 magazine 里的**一个**字符。不是「这个字母全局只能出现一次」，而是**每出现一次只能用一次**。387：`count==1` 从左找，对。日志可定稿。周复盘已代填。上下文窗口仍记：一次请求的 token 上限。


## 遇到的问题（本人填写）

1.

## 遗留问题（本人填写）

- [ ] （有没搞懂的写这里，没有就留空）

## 明日计划（Day 35）

- 休息日
