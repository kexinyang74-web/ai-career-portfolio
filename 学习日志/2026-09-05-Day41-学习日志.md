# 2026-09-05 Day 41 学习日志

> Day 41（第 6 周收官）：力扣 2 道 + 检查点自测 + 周复盘。思考性字段本人填写。不要写密钥。
> **带「代写」的段落待你确认或改成自己的话。**

## 今天做了什么

- （代写）力扣 389（异或 + Counter；方法 3 排序差集未通）、409（补 `has_odd = False` 后本地 7 / 1）。检查点自测 5 题已答。周复盘见下方（代填）。

## LeetCode 思路（每道一句，本人填写）

- （389）（代写）：s 和 t 里成对出现的字母异或会消掉，剩下的 ASCII 就是多出来的那个；Counter 则是比两边次数。
- （409）（代写）：每种字母偶数次数全用进回文，奇数次数先丢掉 1 个凑成偶数；全程最多再加 1 个奇数放中间。`has_odd` 必须在循环前赋成 `False`，否则会 UnboundLocalError。

## 今天写的代码 / 文档

- `projects/python-practice/day41/leetcode_389.py`、`leetcode_409.py`
- `笔记/Day41-检查点自测5题.md`（第 4 题根对象为代补）

## 一个我能讲清楚的原理（本人填写）

（代写）函数里只要后面给某变量赋过值，整段都会当局部变量；用之前必须先赋值。409 的 `has_odd` 就是这个。

## 遇到的问题（本人填写）

1. （代写）409 网站报 `UnboundLocalError: has_odd`：循环里 `if not has_odd` 时还没赋过值。389 方法 3：`sorted` 得到 list，不能 `list - list`。

## 遗留问题（本人填写）

- [ ] （代写，可勾掉）检查点第 3 题 few-shot/CoT 前半仍易和 Day 37 混：few-shot=抄形状，CoT=先走步骤。

## 周复盘（代写，待确认）

1. 本周完成了哪些任务？未完成哪些？
   （代写）任务 1–5 完成：四件套、few-shot/CoT、结构化输出、三版对比、system_prompt v1（正文零改动+版本行）、力扣 389/409。任务 6 本复盘为代填。未完成：本周材料**尚未推 GitHub**（等你发话；`.env` 绝不推）。Day 35 休息跳过。Day 39 实验未钉死同一赛道/条数。
2. 检查点全部通过了吗？卡在哪？
   （代写）6 项均已勾。口头仍易卡：CoT 说成「变准」、few-shot 说成「抗模糊」、json 根必须是对象、对比实验同时改条数。实践卡过：PowerShell 英文引号拆参数；409 未初始化 `has_odd`。
3. 本周最值得写进 GitHub 的产出是什么？（不要提交 `.env`）
   （代写）`projects/project-1-ai-assistant/system_prompt.md` v1 + Day 36–39 对比日志/笔记 + `day38/01_json_mode.py` + `day41` 力扣。不要提交 `.env`、`chat_history.json`。
4. 下周最重要的 3 件事是什么？（第 7 周 = 项目一收尾 / README / 演示）
   （代写）① 项目一 README 写清怎么跑、人设是什么 ② 本地演示路径（交互几条命令，不录也要能讲）③ 简历 STAR 素材从本项目抽三条。不上 RAG。

## 明日计划（Day 42）

- 休息
