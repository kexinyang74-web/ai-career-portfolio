# 2026-09-05 Day 38 学习日志

> Day 38（第 6 周第 3 天）：结构化输出。思考性字段由本人填写。密钥不要写进本文件。

## 今天做了什么

- （待填：列表字段那次 + JSON 是否解析成功）

## 今天写的代码 / 文档

- `projects/python-practice/day38/01_json_mode.py`
- 项目一 `python main.py` 字段约束那次

**1. 列表字段（项目一就能跑，不必改 main.py）**

```powershell
cd C:\Users\Administrator\Desktop\学习安排\projects\project-1-ai-assistant
.\.venv\Scripts\activate
python main.py "只输出 2 条选题。每条必须含三行：标题： 钩子： 人设： 不要前言后记，不要 Markdown 代码块。"
```

```python
标题：从0到1转行AI，我用了这套“反人性”学习法，第30天代码量翻3倍

钩子：你猜我为什么把每天的学习计划精确到“番茄钟”？不是因为自律，是因为我发现，不这么干，我根本坚持不到第30天。今天给你看我的“作弊”记录表。

人设：一个正在转行AI的普通人，分享如何用“系统”对抗“三分钟热度”，把枯燥的代码学习变成每天能上瘾的打卡游戏。        

标题：转行AI第45天，我靠“输出倒逼输入”逼自己读完第6本技术书，还瘦了8斤

钩子：都说转行要“输入”，但我发现，光输入不输出，就是自我感动。所以我给自己定了个变态规矩：每读一章，必须用大白话讲给完全不懂技术的朋友听，讲不明白就重读。结果，书读懂了，连带着晚饭后的久坐时间都戒了。

人设：一个用“转行”倒逼“读书”，再用“读书”倒逼“自律”的普通人，证明知识焦虑和身材焦虑可以一起解决。
```

**2. JSON（概念 + 真跑）**

API 可以设 `response_format: { "type": "json_object" }`，模型被约束成合法 JSON。文档要求：**系统或用户里必须写明要输出 JSON**，否则可能空转/卡住。

项目一骨架 **没有**传这个参数，所以今天用 `day38/01_json_mode.py`：在 body 里打开 json_object，user 里明确要一个 JSON 对象（一个根对象，里面是数组也可以包在键里）。

```powershell
cd C:\Users\Administrator\Desktop\学习安排\projects\python-practice\day38
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
Copy-Item ..\..\project-1-ai-assistant\.env .env
python 01_json_mode.py
```

```python
原始 content:
{"items":[{"title":"从零开始学编程的第一周","hook":"当你的生活变成一行行代码，连买菜都要用算法规划路线。","fit":" 像生活记录一样，记录了一个普通人如何将日常琐事转化为编程学习的真实片段。"},{"title":"转行程序员后的第一个项目","hook":"从写简历到写代码，你的人生终于有了自己的版本控制。","fit":"像生活记录一样，捕捉了转行过程中那些微小却决定性的瞬间，真实而共鸣。"}]}
---
解析成功，类型: dict
顶层键: ['items']
{
  "items": [
    {
      "title": "从零开始学编程的第一周",
      "hook": "当你的生活变成一行行代码，连买菜都要用算法规划路线。",
      "fit": "像生活记录一样，记录了一个普通人如何将日常琐事转化为编程学习的真实片段。"
    },
    {
      "title": "转行程序员后的第一个项目",
      "hook": "从写简历到写代码，你的人生终于有了自己的版本控制。",
      "fit": "像生活记录一样，捕捉了转行过程中那些微小却决定性的瞬间，真实而共鸣。"
    }
  ]
}
```
## 一个我能讲清楚的原理（本人填写）

（例如：为什么 json_object 还要在 messages 里写要 JSON？字段和感觉「写得好」有何不同？）
**为什么不能只开模式？** 官方文档说明：没有明确的 JSON 指令时，模型可能持续生成空白字符，直到耗尽 token。为防止这种情况，API 会检查上下文中是否出现 `JSON`，没有就报错。

> **批注（Claude）**：JSON 解析成功 ✅。字段齐 ≠ 内容更好。自测 5/5。列表那次三行标签也齐。日志可定稿。

## 遇到的问题（本人填写）

1.

## 遗留问题（本人填写）

- [ ] （有没搞懂的写这里，没有就留空）

## 明日计划（Day 39）

- 同一任务亲手写 3 版 prompt，记差异
