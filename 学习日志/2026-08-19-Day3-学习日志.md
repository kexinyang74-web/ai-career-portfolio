# 2026-08-19 Day 3 学习日志

> Day 3：学会"读"Git 状态（status/log/diff）+ AI 代跑完整流程。直接在下面填写今天真实完成的内容。

## 今天做了什么

- 学会读 `git status`（分支、ahead/behind、modified/untracked、staged）
- 学会读 `git log --oneline`（提交编号、HEAD、origin/main 对比）
- 学会读 `git diff`（`-`/`+` 行、`@@` 位置标记）
- AI 代跑完整流程：add → commit → branch → clone，逐段读懂每一步输出
- 教学笔记：笔记/Day3-读懂Git状态.md

## 遇到的问题与解决

- （例如：untracked 的新文件在 `git diff` 里看不到，需要 add 后用 `git diff --cached`）

## 今天写的代码 / 文档

- 笔记/Day3-读懂Git状态.md

## 一个我能讲清楚的原理

- （例如：commit 只存本地、push 才上传；分支只是指向某个提交的指针）

## 还没搞懂的部分

- （如果有，写下来下次解决）

## 明日计划（Day 4）

- 安装 Python 3.12+ / VS Code，学会 `venv` 与 `pip`
- 学会 `.env` 密钥管理，配置 `.gitignore`
