# 2026-09-04 Day 31 学习日志

> Day 31（第 5 周第 3 天）：多轮 messages。思考性字段由本人填写。密钥不要写进本文件。

## 今天做了什么

- 多轮messages
- （代填补漏）跑通 `01_multi_turn.py` 两次 POST；第二次能用到第一轮名字；`prompt_tokens` 21→43；自测 5/5

## 今天写的代码 / 文档

- `projects/python-practice/day31/01_multi_turn.py`

## 一个我能讲清楚的原理（本人填写）

（例如：为什么要把 assistant 追加进 messages？两次 prompt_tokens 为什么不同？）

1. 要把 assistant 追加进 messages的原因是，assistant是模型的回答，如果不加进去，模型不知道上轮回答了什么。相当于没有获得更多有效信息，
2. （代填补漏）第二次 `prompt_tokens` 更大，因为 `messages` 更长：第一轮的 user + assistant + 新的 user 都要再寄一遍。

> **批注（Claude）**：原理 1 ✅。补一句精度：不加 assistant 时，**第一句 user 里的名字往往还在**，模型仍可能答出「小周」；缺的是「上轮自己说过什么」。`/new` 只剩 system 人设。日志可定稿。
## 遇到的问题（本人填写）



## 遗留问题（本人填写）

- [ ] （有没搞懂的写这里，没有就留空）

## 明日计划（Day 32）

- 流式：`stream=true`，拼 `delta.content` 直到 `[DONE]`
