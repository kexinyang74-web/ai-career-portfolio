# 2026-09-02 Day 25 学习日志

> Day 25(第 4 周第 4 天):pytest 测试基础(断言/测试函数,5+ 测试)。思考性字段由本人填写(或委托 Claude 代填)。

## 今天做了什么

- 练习文件(projects/python-practice/day25/):
  - 环境:day25 新建 .venv,pytest 9.1.1 + requirements.txt(6 个包:pytest 直接依赖 + colorama/iniconfig/packaging/pluggy/Pygments 间接 5 个)
  - score_tools.py(被测:计算平均分/转换等级/过滤不及格)+ test_score_tools.py(7 个测试)→ 首跑全绿 `7 passed` ✅
  - 意外收获:docstring 里写 `\.venv\Scripts\` 触发 `SyntaxWarning: invalid escape sequence`(Python 字符串反斜杠=转义符)→ 路径改正斜杠 → warning 消失
  - 🧨 破坏实验:把 `total += score` 改成 `total = score`(Day 24 同款 bug)→ 跑出 `1 failed, 6 passed` → 学会读失败输出(实际 33.33 ≠ 期望 90.0,E 行 + where 行)→ 改回 `+=` → 全绿,红绿闭环完成
- LeetCode:本周 2 道在 Day 26-27 做(每周 2 道新节奏)

## LeetCode 思路(每道一句,本人填写)

- (本周力扣在 Day 26-27 做)

## 今天写的代码 / 文档

- projects/python-practice/day25/(score_tools.py、test_score_tools.py、requirements.txt)
- 笔记/Day25-pytest测试基础.md

## 一个我能讲清楚的原理(本人填写)

(例如:pytest 是什么?assert 怎么写?失败输出怎么读?为什么只挂 1 个?)
1.
2.
3.

## 遇到的问题(本人填写)

1. **pytest 是哪个?是 test_score_tools.py 吗?**(概念混淆):pytest 是**工具/命令**(装在 venv 里,自动扫描并运行 test_ 开头的文件),test_score_tools.py 只是被它运行的测试文件之一。分清三样东西:pytest(考官)/ 测试文件(考卷)/ 被测代码(考生)。
2. **docstring 里反斜杠路径触发 SyntaxWarning**:Python 字符串里 `\` 是转义符,`\.` `\S` 不合法 → 路径改正斜杠(终端命令里反斜杠照旧)。

> **批注(Claude)**:「今天做了什么/遇到的问题」按真实过程代填,请核对。今天最值得记住的一句话:**红绿闭环 = 测试的价值所在:改坏→红→修好→绿,以后每次改动都这么兜底。**

## 遗留问题(本人填写)

- [ ] (有没搞懂的写这里,没有就留空)

## 明日计划(Day 26)

- 小脚本 3:读 CSV/Excel → 清洗 → 统计报告(第 4 周检查点脚本,需"有测试"——pytest 今天学的直接上场)
